# ============================================
# EigenPoly Backend — Environment Variables
# ============================================

# --- Required ---

# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host:5432/eigenpoly

# Polygon RPC (Chainstack recommended for reliability)
CHAINSTACK_NODE=https://polygon-mainnet.core.chainstack.com/YOUR_KEY

# LLM API key for market analysis (OpenRouter)
OPENROUTER_API_KEY=sk-or-...

# --- TEE (auto-injected by EigenCompute KMS) ---
# Do NOT set this manually. The TEE injects it at runtime.
# MNEMONIC=

# --- Polymarket Builder Relayer (optional, for gasless orders) ---
BUILDER_API_KEY=
BUILDER_SECRET=
BUILDER_PASS_PHRASE=

# --- Fallback (local dev only, not needed in TEE) ---
# POLYCLAW_PRIVATE_KEY=0x...

# --- Optional ---

# Rotating proxy for CLOB API (avoids Cloudflare blocks)
# HTTPS_PROXY=socks5://user:pass@host:port

# Max retries for Cloudflare blocks
# CLOB_MAX_RETRIES=5
